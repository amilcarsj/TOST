from backend.db.common_db_requests import get_all_trip_ids

# def populate_trip_table()
#   tripsIds = load_trip_ids()
#   for trip_id in trip_ids:
#     trip = load_trip(trip_id)
#     remove_rows_with_duplicated_timestamp(trip)
#     remove_outliers(trip)
#     interpolate(trip)
#     calculate_speed_bearing(trip)
#     save_trip(trip)
